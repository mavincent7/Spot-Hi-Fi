myApp.addModule.apply(myApp.metier,["regexUtil", function(){	var regexDate = /^([0-9]{4})$/;	var regexLatin1 = /^[a-zA-ZÀÁÂÃÄÅÆÇÈÉÊËÌÍÎÏÐÑÒÓÔÕÖ×ØÙÚÛÜÝÞßàáâãäåæ¸èéêöìíîïðñòóôõö÷øùúûAaüýþÿ\s"'\-]*$/i;	var regexLatin1WithDigits = /^[a-zA-ZÀÁÂÃÄÅÆÇÈÉÊËÌÍÎÏÐÑÒÓÔÕÖ×ØÙÚÛÜÝÞßàáâãäåæ¸èéêöìíîïðñòóôõö÷øùúûAaüýþÿ\s"'\-0-9]*$/i;	var regexLatin1WithDigitsPunctuation = /^[a-zA-ZÀÁÂÃÄÅÆÇÈÉÊËÌÍÎÏÐÑÒÓÔÕÖ×ØÙÚÛÜÝÞßàáâãäåæ¸èéêöìíîïðñòóôõö÷øùúûAaüýþÿ\s"'\-0-9\;\.\,\!\?\:\(\)]*$/i;	var validateRegex = function(spec){		if (typeof spec.chaine === "string" && (! spec.minLength || spec.chaine.length >= spec.minLength) && (!spec.maxLength || spec.chaine.length <= spec.maxLength)){			return spec.regex.test(spec.chaine);		} 		return false ;	}; 	var publicInterfaceRegex = {   		testRegexDate : function(spec){			spec.regex = regexDate ;			return validateRegex(spec);		}, 		testRegexLatin1 : function(spec){			spec.regex = regexLatin1 ; 			return validateRegex(spec);		}, 		testRegexLatin1WithDigits : function(spec){			spec.regex = regexLatin1WithDigits ; 			return validateRegex(spec);		}, 		testRegexLatin1WithDigitsPunctuation : function(spec){			spec.regex = regexLatin1WithDigitsPunctuation;			return validateRegex(spec);		}	};	return publicInterfaceRegex ;}()]);